<article class="page">
  <% if (frontMatter.title) {%>
  <h1><%- frontMatter.title %></h1>
  <% if (postedDate) {%><small><%- `Posted: ${postedDate}` %></small><% } %> <%
  } %><%- typeof html != 'undefined' ? html : ''%>
</article>
